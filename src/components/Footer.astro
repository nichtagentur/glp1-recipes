---
const base = import.meta.env.BASE_URL;
const year = new Date().getFullYear();
const categories = [
  'breakfast', 'lunch', 'dinner', 'snacks',
  'soups', 'smoothies', 'meal-prep', 'side-dishes'
];
---

<footer class="relative mt-32">
  <!-- Disclaimer bar -->
  <div class="bg-rose/20 border-y border-rose/30 text-cream/80 text-center py-3 px-6 text-[13px] font-light tracking-wide">
    <span class="text-rose font-medium">Medical Disclaimer</span>
    <span class="mx-2 text-cream/30">|</span>
    This site provides nutrition information only, not medical advice.
    <a href={`${base}/disclaimer/`} class="text-amber hover:text-amber-light underline underline-offset-2 ml-1 transition-colors">Read full disclaimer</a>
  </div>

  <!-- Footer content -->
  <div class="bg-charcoal border-t border-cream/[0.06]">
    <div class="max-w-7xl mx-auto px-6 py-16 grid grid-cols-1 md:grid-cols-12 gap-12">
      <!-- Brand -->
      <div class="md:col-span-5">
        <div class="flex items-baseline gap-2 mb-4">
          <span class="font-display text-xl font-bold text-cream">GLP-1</span>
          <span class="font-display text-xs text-cream-muted italic tracking-widest uppercase">Recipes</span>
        </div>
        <p class="text-cream-muted text-sm leading-relaxed max-w-sm font-light">
          High-protein, portion-controlled recipes for people on GLP-1 medications.
          Every recipe: 20-40g protein, 300-500 calories. Designed to nourish, not overwhelm.
        </p>
      </div>

      <!-- Categories -->
      <div class="md:col-span-4">
        <h3 class="text-[11px] text-amber font-medium tracking-[0.2em] uppercase mb-5">Categories</h3>
        <div class="grid grid-cols-2 gap-x-8 gap-y-2">
          {categories.map(cat => (
            <a
              href={`${base}/category/${cat}/`}
              class="text-sm text-cream-muted hover:text-amber capitalize font-light transition-colors duration-300"
            >
              {cat.replace('-', ' ')}
            </a>
          ))}
        </div>
      </div>

      <!-- Links -->
      <div class="md:col-span-3">
        <h3 class="text-[11px] text-amber font-medium tracking-[0.2em] uppercase mb-5">Resources</h3>
        <div class="flex flex-col gap-2">
          <a href={`${base}/about/`} class="text-sm text-cream-muted hover:text-amber font-light transition-colors duration-300">About Us</a>
          <a href={`${base}/disclaimer/`} class="text-sm text-cream-muted hover:text-amber font-light transition-colors duration-300">Disclaimer</a>
          <a href="https://www.niddk.nih.gov/" rel="noopener" target="_blank" class="text-sm text-cream-muted hover:text-amber font-light transition-colors duration-300">NIH Resources</a>
        </div>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="border-t border-cream/[0.04] text-center py-5">
      <p class="text-[11px] text-cream-muted/40 tracking-widest uppercase font-light">
        &copy; {year} GLP-1 Recipes &mdash; For informational purposes only
      </p>
    </div>
  </div>
</footer>
